These files were moved from the workspace root into `website/legacy-root` so the active project stays inside one folder.

Nothing in this folder is part of the current `website` build unless you intentionally bring it back.
